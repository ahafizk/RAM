Feature Extraction:

Run the following command:
    python FeatureExtractor.py <directory_name>

--> <directory_name> is the path to directory which contains radar data

The above command will:
1. Generate CSV files
2. Extract Features and store them in a 'features' directory in provided path
3. Combine all features by radar and store them in a "Results folder"

